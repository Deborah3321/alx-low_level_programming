#include "main.h"

/**
 * _islower - checks for lowercase characters
 * @c: character to be checked
 * Return: 1 for lowercase, 0 otherwise
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
