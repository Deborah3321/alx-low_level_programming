#include "main.h"
/**
 * _abs - calculates absolute value of an integer
 * @n: integer to be calculated on
 * Return: absolute value of the integer
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
