#include <stdlib.h>
#include <stdio.h>

/**
 * rem - finds remainder of i divided by j
 * @i: first integer
 * @j: second integer
 * Return: rem of two integers
 */
int rem(int i, int j)
{
	return(i % j);
}
