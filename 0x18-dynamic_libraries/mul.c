#include <stdlib.h>
#include <stdio.h>

/**
 * mul - multiplies two integers
 * @i: first integer
 * @j: second integer
 * Return: product of two integers
 */
int mul(int i, int j)
{
	return(i * j);
}
